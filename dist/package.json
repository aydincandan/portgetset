{
  "name": "portgetset",
  "version": "1.0.3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["new", "port", "automatic", "get", "set", "cli", "node"],
  "author": "aydincandan@gmail.com",
  "license": "ISC",
  "description": "First, PORT=xxx defined in the .env file is taken. If this is not present, the first parameter read from the command line is accepted as PORT.",
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "terser": "^5.37.0"
  }
}
